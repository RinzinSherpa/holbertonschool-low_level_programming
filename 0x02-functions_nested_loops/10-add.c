#include "holberton.h"

/**
 * add - a function that adds two integer and returns the result
 * @x: first parameter
 * @y: second parameter
 * Return: return type is int
 */
int add(int x, int y)
{
	int z;

	z = x + y;
	return (z);
}
