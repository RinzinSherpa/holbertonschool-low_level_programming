#include "holberton.h"
/**
 * mul - function that multiplies two integers
 * @a: parameter is a
 * @b: parameter is b
 * Return: return type is int
*/
int mul(int a, int b)
{
	int z;

	z = (a * b);
	return (z);
}
